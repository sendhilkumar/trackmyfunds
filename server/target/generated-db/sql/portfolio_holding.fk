alter table portfolio_holding add constraint portfolio_holding_fk_0 foreign key (
    portfolio_id
)
references portfolio(
    id
);

alter table portfolio_holding add constraint portfolio_holding_fk_1 foreign key (
    scheme_code
)
references scheme(
    code
);

